// CJS vi.fn() stub for the ESM-only archiver package.
// `vi` is a global injected by Vitest into the VM context; no import needed.
// eslint-disable-next-line no-undef
const mockFn = vi.fn();
module.exports = mockFn;
